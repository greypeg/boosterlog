import { NextPage } from "next";
import { PageLayout } from "~/components/page-layout";

const Boosters: NextPage = () => {
    return (
        <PageLayout>
            
        </PageLayout>
    );
};

export default Boosters;